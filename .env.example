# Database Configuration
DB_HOST=localhost
DB_PORT=5433
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=internal_transfers
DB_SSLMODE=disable

# Server Configuration
PORT=8080
GIN_MODE=debug

# Liquibase Configuration (for manual runs)
LIQUIBASE_URL=jdbc:postgresql://localhost:5432/internal_transfers
LIQUIBASE_USERNAME=postgres
LIQUIBASE_PASSWORD=password